{
    "name": "notehub.md",
    "version": "0.1.6",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "pnpm --filter @notehub.md/api build && pnpm --filter @notehub.md/cli build && node scripts/ensure-cli.js && pnpm -r build",
        "dev": "pnpm -r --parallel dev",
        "clean": "pnpm -r clean",
        "gen:plugin": "tsx scripts/create-plugin.ts",
        "predev": "tsx scripts/link-plugins.ts",
        "dev:desktop": "pnpm predev && pnpm --filter @notehub/desktop tauri dev",
        "dev:desktop:debug": "pnpm build && pnpm predev && pnpm --filter @notehub/desktop tauri dev",
        "dev:android": "pnpm predev && pnpm --filter @notehub/desktop tauri android dev",
        "dev:android:debug": "pnpm build && pnpm predev && pnpm --filter @notehub/capacitor exec cap sync && pnpm --filter @notehub/capacitor exec cap run android",
        "android:release": "tsx scripts/build-android-release.ts",
        "cap:release": "tsx scripts/build-capacitor-release.ts",
        "build:desktop": "pnpm build && pnpm predev && pnpm --filter @notehub/desktop tauri build"
    },
    "devDependencies": {
        "@types/prompts": "^2.4.9",
        "esbuild": "^0.27.2",
        "fast-glob": "^3.3.3",
        "prompts": "^2.4.2",
        "tsx": "^4.21.0",
        "typescript": "^5.3.0"
    },
    "packageManager": "pnpm@9.0.0"
}