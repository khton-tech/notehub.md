/**
 * @fileoverview Re-export of API contract from @notehub/api
 * @module @notehub/core/api/contract
 *
 * This file re-exports the API contract types from the public SDK package.
 * The canonical source of truth for API definitions is now @notehub/api.
 *
 * @deprecated Import from '@notehub/api' directly for new code.
 * This re-export exists for backward compatibility with internal plugins.
 */

export * from '@notehub/api';
